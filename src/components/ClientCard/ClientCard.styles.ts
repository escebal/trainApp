import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    clientCardContainer: {
    backgroundColor: "#eee",
    flexDirection: 'row',
    margin: 5,
    padding: 20
  },
});

export default styles;
